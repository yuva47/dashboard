import { DashletDirective } from './dashlet.directive';

describe('DashletDirective', () => {
  it('should create an instance', () => {
    const directive = new DashletDirective();
    expect(directive).toBeTruthy();
  });
});
